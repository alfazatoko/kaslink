rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{colId}/{docId} {
      allow read, write: if request.auth != null && colId.endsWith(request.auth.uid);
    }


  }
}
